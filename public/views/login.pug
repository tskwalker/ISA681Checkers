extends base.pug

block body
    h3 Login to checkers Web Page

    .errorDiv
      if(error)
        p #{error}

    form(method='POST' action='/login' autocomplete='off')
        div.form-group
            label(for='username') User name:
            input#username.form-control(type='text', placeholder='email used to register' name='username')
        .form-group
            label(for='password') Password:
            input#password.form-control(type="password" name="password",autocomplete='off')

        input(name='_csrf' value= csrfToken)
        button.btn.btn-primary(type='submit') Submit
    