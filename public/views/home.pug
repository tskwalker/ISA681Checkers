html
    head
        title #{title}
        link(rel='stylesheet', href='/stylesheets/app.css')
        script(src='/socket.io/socket.io.js')
        script(src="/jquery/jquery.js")
        script(src='javascripts/checkersClient.js')
        script(src="/tabulator/js/tabulator.min.js")
        link(href="/tabulator/css/tabulator.min.css" rel="stylesheet")
    body
     h1 Welcome #{name}
    
    #content(style="text-align: right;float: right;")
        -url="/logout"
        a(href=url style='align:right') Logout
    
    // pulled from login page
    p(hidden)#sEmail #{email}
    p(hidden)#sName #{name}
    p(hidden)#pId #{playerId}

    #content
        .options
            button#createGRBtn Create Game Room
            button#joinGRBtn Join Game Room
            button#moveGBtn Game Moves
            button#gameHistory Game History
            button#clearTables Hide Stats

    p(hidden).waitMsg.
        Waiting for second Player
  
    p(hidden).playerJoinedMsg.
        New Player Joined.
    p(hidden).joinRoomMsg
        | Select a Game Room to join.

    #roomList
      .availList
           button(hidden)#jRoomBtn Join

    p(hidden).selectRoomMsg
        | Select a Game Room to view moves.

    #completedGames
        .rooms 
            button(hidden)#cRoomId

    
    #stat_table.stat_data
    br
    br
    p(hidden).movesMsg Game Moves
    #moves_table.moves

    #content
        .checkers
            button#startPlayBtn(type="button") StartPlay
            //a(href="/checkersGame")

    #currentMoves

    
    .column
            .info
                h1 Checkers
                hr
                p
                    | ISA 681 Checkers Game Project
                #iam #{name}
                
                
                
            .stats
                h2 Game Room Information 
                p Game Room : 
                    #gameRoom  #{gameRoom}
                .wrapper
                    #player1
                        h3 Player 1 (Top)
                        #player1Id #{player1}
                        //#pId #{}
                    #player2
                        h3 Player 2 (Bottom)
                        #player2Id #{player2}
                .clearfix
                .turn
                button#pausegame Pause Game
            .column
                #board
                    .tiles
                    .pieces
                        .player1pieces
                        .player2pieces
            .hiddens
                p(hidden)#myEmail #{my_email}


